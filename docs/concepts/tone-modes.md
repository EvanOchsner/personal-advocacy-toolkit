# Tone modes

*(Stub — authored in Phase 4A from the portable "Tone modes" section of the
source project's `CLAUDE.md`. Nothing case-specific crosses.)*

Two registers: **lawyer mode** (careful, precise, on-record) and **casual
mode** (normal-person-to-normal-person). Plus the when-to-use-which rules,
the read-aloud test, and the "scripts are scaffolds, not recitation"
convention.
