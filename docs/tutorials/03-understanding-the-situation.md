# Tutorial 03: Understanding the situation

*(Stub — authored in Phase 5.)*

Uses `situation_classify`, `authorities_lookup`, and `deadline_calc` to
triage the case: what kind of dispute is this, who has jurisdiction,
what deadlines are live. Outputs feed into the playbook selection and
the packet manifest.
