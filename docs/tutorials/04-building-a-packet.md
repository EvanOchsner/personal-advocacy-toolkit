# Tutorial 04: Building a packet

*(Stub — authored in Phase 5.)*

Writing a `packet-manifest.yaml`, running `scripts/packet/build.py`,
validating with `packet_manifest_validate.py`, and confirming every
artifact in the packet traces to a hashed source via `provenance.py`.
