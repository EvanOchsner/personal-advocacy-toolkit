# provenance skill — port pending

The canonical authored `SKILL.md` for this skill lives in a private
project (`lucy-repair-fight/.claude/skills/provenance/`) that Phase
4A (Agent 4A) did not have read access to. The current `SKILL.md`
in this directory is a stub that describes the intent and points at
the already-in-tree scripts (`scripts/provenance.py`,
`scripts/provenance_snapshot.py`, `scripts/evidence_hash.py`).

## Follow-up task

Pull the full `SKILL.md` contents from the source repo, update any
references that still point at the private tree, and replace the
stub in place. Delete this README once the port lands.
